apply plugin: 'android-library'

dependencies {
    compile 'com.android.support:support-v4:13.0.+'
    compile files('./libs/bolts.jar')
}

android {
    compileSdkVersion 19 //Integer.parseInt(project.ANDROID_BUILD_SDK_VERSION)
    buildToolsVersion "19.1.0" //project.ANDROID_BUILD_TOOLS_VERSION

    defaultConfig {
        minSdkVersion 14 //Integer.parseInt(project.ANDROID_BUILD_MIN_SDK_VERSION)
        targetSdkVersion 19 //Integer.parseInt(project.ANDROID_BUILD_TARGET_SDK_VERSION)
    }

    lintOptions {
        abortOnError false
    }

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
        }
    }
}
